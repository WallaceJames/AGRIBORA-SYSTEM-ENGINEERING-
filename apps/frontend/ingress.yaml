apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: hrms-frontend
  namespace: hrms
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`hrms.local`)
      kind: Rule
      services:
        - name: hrms-frontend
          port: 80
