apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-frontend-to-redis
  namespace: hrms
spec:
  podSelector:
    matchLabels:
      app: hrms-redis
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: hrms-frontend
      ports:
        - port: 6379
